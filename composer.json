{
    "name": "phgraph/graph",
    "description": "Mathematical Graphing Library",
    "type": "library",
    "license": "MIT",
    "keywords": [
        "graph",
        "network",
        "mathematical",
        "vertex",
        "edge"
    ],
    "homepage": "https://github.com/phgraph/graph",
    "authors": [
        {
            "name": "sporchia",
            "email": "print@blindchaos.net"
        }
    ],
    "require": {
        "php": "^7.2.0",
        "symfony/process": "^5.0"
    },
    "autoload": {
        "psr-4": {
            "PHGraph\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "require-dev": {
        "phpstan/phpstan": "^0.12.4",
        "phpunit/phpunit": "^8.0",
        "php-coveralls/php-coveralls": "^2.1",
        "mockery/mockery": "^1.2",
        "phpstan/phpstan-mockery": "^0.12.3"
    },
    "scripts": {
        "analyse": [
            "./vendor/bin/phpstan analyse --ansi"
        ],
        "coverage": [
            "./vendor/bin/phpunit --colors=always --coverage-clover=./report/coverage.clover --coverage-html=./report"
        ],
        "test": [
            "./vendor/bin/phpunit --colors=always"
        ]
    }
}
